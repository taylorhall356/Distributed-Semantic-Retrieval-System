services:
  bootstrap:
    build:
      context: .
    container_name: semantic-retrieval-bootstrap
    command: python bootstrap.py
    depends_on:
      embedding-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
      minio:
        condition: service_started
    environment:
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: semantic_retrieval
      DB_USER: semantic_user
      DB_PASSWORD: semantic_password
      STORAGE_BACKEND: minio
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_BUCKET: document-storage
      MINIO_SECURE: "false"
      QDRANT_HOST: qdrant
      QDRANT_PORT: "6333"
      QDRANT_COLLECTION: document_chunks
      EMBEDDING_VECTOR_SIZE: "384"

  embedding-service:
    build:
      context: .
    container_name: semantic-retrieval-embedding-service
    restart: unless-stopped
    command: uvicorn embedding_service:app --host 0.0.0.0 --port 8090
    environment:
      EMBEDDING_MODEL_NAME: sentence-transformers/all-MiniLM-L6-v2
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8090/health', timeout=5).read()"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 180s

  nginx:
    image: nginx:1.27-alpine
    container_name: semantic-retrieval-nginx
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
      api2:
        condition: service_healthy
      api3:
        condition: service_healthy
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1/ready >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 45s

  api:
    build:
      context: .
    container_name: semantic-retrieval-api
    restart: unless-stopped
    depends_on:
      bootstrap:
        condition: service_completed_successfully
      embedding-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_started
    environment:
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: semantic_retrieval
      DB_USER: semantic_user
      DB_PASSWORD: semantic_password
      JWT_SECRET: development-secret-key-at-least-32-bytes
      STORAGE_BACKEND: minio
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_BUCKET: document-storage
      MINIO_SECURE: "false"
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: "5672"
      RABBITMQ_USER: guest
      RABBITMQ_PASSWORD: guest
      DOCUMENT_PROCESSING_QUEUE: document_processing
      DOCUMENT_PARSING_QUEUE: document_parsing
      DOCUMENT_EMBEDDING_QUEUE: document_embedding
      QDRANT_HOST: qdrant
      QDRANT_PORT: "6333"
      QDRANT_COLLECTION: document_chunks
      EMBEDDING_MODEL_NAME: sentence-transformers/all-MiniLM-L6-v2
      EMBEDDING_VECTOR_SIZE: "384"
      EMBEDDING_SERVICE_URL: http://embedding-service:8090
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DB: "0"
      QUERY_EMBEDDING_CACHE_TTL_SECONDS: "3600"
      QUERY_EMBEDDING_CACHE_LOGGING: "true"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=5).read()"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 120s

  api2:
    build:
      context: .
    container_name: semantic-retrieval-api-2
    restart: unless-stopped
    depends_on:
      bootstrap:
        condition: service_completed_successfully
      embedding-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_started
    environment:
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: semantic_retrieval
      DB_USER: semantic_user
      DB_PASSWORD: semantic_password
      JWT_SECRET: development-secret-key-at-least-32-bytes
      STORAGE_BACKEND: minio
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_BUCKET: document-storage
      MINIO_SECURE: "false"
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: "5672"
      RABBITMQ_USER: guest
      RABBITMQ_PASSWORD: guest
      DOCUMENT_PROCESSING_QUEUE: document_processing
      DOCUMENT_PARSING_QUEUE: document_parsing
      DOCUMENT_EMBEDDING_QUEUE: document_embedding
      QDRANT_HOST: qdrant
      QDRANT_PORT: "6333"
      QDRANT_COLLECTION: document_chunks
      EMBEDDING_MODEL_NAME: sentence-transformers/all-MiniLM-L6-v2
      EMBEDDING_VECTOR_SIZE: "384"
      EMBEDDING_SERVICE_URL: http://embedding-service:8090
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DB: "0"
      QUERY_EMBEDDING_CACHE_TTL_SECONDS: "3600"
      QUERY_EMBEDDING_CACHE_LOGGING: "true"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=5).read()"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 120s

  api3:
    build:
      context: .
    container_name: semantic-retrieval-api-3
    restart: unless-stopped
    depends_on:
      bootstrap:
        condition: service_completed_successfully
      embedding-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_started
    environment:
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: semantic_retrieval
      DB_USER: semantic_user
      DB_PASSWORD: semantic_password
      JWT_SECRET: development-secret-key-at-least-32-bytes
      STORAGE_BACKEND: minio
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_BUCKET: document-storage
      MINIO_SECURE: "false"
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: "5672"
      RABBITMQ_USER: guest
      RABBITMQ_PASSWORD: guest
      DOCUMENT_PROCESSING_QUEUE: document_processing
      DOCUMENT_PARSING_QUEUE: document_parsing
      DOCUMENT_EMBEDDING_QUEUE: document_embedding
      QDRANT_HOST: qdrant
      QDRANT_PORT: "6333"
      QDRANT_COLLECTION: document_chunks
      EMBEDDING_MODEL_NAME: sentence-transformers/all-MiniLM-L6-v2
      EMBEDDING_VECTOR_SIZE: "384"
      EMBEDDING_SERVICE_URL: http://embedding-service:8090
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DB: "0"
      QUERY_EMBEDDING_CACHE_TTL_SECONDS: "3600"
      QUERY_EMBEDDING_CACHE_LOGGING: "true"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=5).read()"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 120s

  worker:
    build:
      context: .
    container_name: semantic-retrieval-worker
    restart: unless-stopped
    command:
      - sh
      - -c
      - >
        celery -A tasks worker
        --loglevel=info
        --pool="$${CELERY_WORKER_POOL}"
        --concurrency="$${CELERY_WORKER_CONCURRENCY}"
        --queues="$${DOCUMENT_PARSING_QUEUE}"
    depends_on:
      bootstrap:
        condition: service_completed_successfully
      embedding-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_started
    environment:
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: semantic_retrieval
      DB_USER: semantic_user
      DB_PASSWORD: semantic_password
      JWT_SECRET: development-secret-key-at-least-32-bytes
      STORAGE_BACKEND: minio
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_BUCKET: document-storage
      MINIO_SECURE: "false"
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: "5672"
      RABBITMQ_USER: guest
      RABBITMQ_PASSWORD: guest
      DOCUMENT_PROCESSING_QUEUE: document_processing
      DOCUMENT_PARSING_QUEUE: document_parsing
      DOCUMENT_EMBEDDING_QUEUE: document_embedding
      QDRANT_HOST: qdrant
      QDRANT_PORT: "6333"
      QDRANT_COLLECTION: document_chunks
      EMBEDDING_MODEL_NAME: sentence-transformers/all-MiniLM-L6-v2
      EMBEDDING_VECTOR_SIZE: "384"
      EMBEDDING_SERVICE_URL: http://embedding-service:8090
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DB: "0"
      QUERY_EMBEDDING_CACHE_TTL_SECONDS: "3600"
      QUERY_EMBEDDING_CACHE_LOGGING: "true"
      CELERY_WORKER_POOL: prefork
      CELERY_WORKER_CONCURRENCY: "2"
      WORKER_ROLE: parsing

  worker2:
    build:
      context: .
    container_name: semantic-retrieval-worker-2
    restart: unless-stopped
    command:
      - sh
      - -c
      - >
        celery -A tasks worker
        --loglevel=info
        --pool="$${CELERY_WORKER_POOL}"
        --concurrency="$${CELERY_WORKER_CONCURRENCY}"
        --queues="$${DOCUMENT_PARSING_QUEUE}"
    depends_on:
      bootstrap:
        condition: service_completed_successfully
      embedding-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_started
    environment:
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: semantic_retrieval
      DB_USER: semantic_user
      DB_PASSWORD: semantic_password
      JWT_SECRET: development-secret-key-at-least-32-bytes
      STORAGE_BACKEND: minio
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_BUCKET: document-storage
      MINIO_SECURE: "false"
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: "5672"
      RABBITMQ_USER: guest
      RABBITMQ_PASSWORD: guest
      DOCUMENT_PROCESSING_QUEUE: document_processing
      DOCUMENT_PARSING_QUEUE: document_parsing
      DOCUMENT_EMBEDDING_QUEUE: document_embedding
      QDRANT_HOST: qdrant
      QDRANT_PORT: "6333"
      QDRANT_COLLECTION: document_chunks
      EMBEDDING_MODEL_NAME: sentence-transformers/all-MiniLM-L6-v2
      EMBEDDING_VECTOR_SIZE: "384"
      EMBEDDING_SERVICE_URL: http://embedding-service:8090
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DB: "0"
      QUERY_EMBEDDING_CACHE_TTL_SECONDS: "3600"
      QUERY_EMBEDDING_CACHE_LOGGING: "true"
      CELERY_WORKER_POOL: prefork
      CELERY_WORKER_CONCURRENCY: "2"
      WORKER_ROLE: parsing

  worker3:
    build:
      context: .
    container_name: semantic-retrieval-worker-3
    restart: unless-stopped
    command:
      - sh
      - -c
      - >
        celery -A tasks worker
        --loglevel=info
        --pool="$${CELERY_WORKER_POOL}"
        --concurrency="$${CELERY_WORKER_CONCURRENCY}"
        --queues="$${DOCUMENT_PARSING_QUEUE}"
    depends_on:
      bootstrap:
        condition: service_completed_successfully
      embedding-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_started
    environment:
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: semantic_retrieval
      DB_USER: semantic_user
      DB_PASSWORD: semantic_password
      JWT_SECRET: development-secret-key-at-least-32-bytes
      STORAGE_BACKEND: minio
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_BUCKET: document-storage
      MINIO_SECURE: "false"
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: "5672"
      RABBITMQ_USER: guest
      RABBITMQ_PASSWORD: guest
      DOCUMENT_PROCESSING_QUEUE: document_processing
      DOCUMENT_PARSING_QUEUE: document_parsing
      DOCUMENT_EMBEDDING_QUEUE: document_embedding
      QDRANT_HOST: qdrant
      QDRANT_PORT: "6333"
      QDRANT_COLLECTION: document_chunks
      EMBEDDING_MODEL_NAME: sentence-transformers/all-MiniLM-L6-v2
      EMBEDDING_VECTOR_SIZE: "384"
      EMBEDDING_SERVICE_URL: http://embedding-service:8090
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DB: "0"
      QUERY_EMBEDDING_CACHE_TTL_SECONDS: "3600"
      QUERY_EMBEDDING_CACHE_LOGGING: "true"
      CELERY_WORKER_POOL: prefork
      CELERY_WORKER_CONCURRENCY: "2"
      WORKER_ROLE: parsing

  worker4:
    build:
      context: .
    container_name: semantic-retrieval-worker-4
    restart: unless-stopped
    command:
      - sh
      - -c
      - >
        celery -A tasks worker
        --loglevel=info
        --pool="$${CELERY_WORKER_POOL}"
        --concurrency="$${CELERY_WORKER_CONCURRENCY}"
        --queues="$${DOCUMENT_PARSING_QUEUE}"
    depends_on:
      bootstrap:
        condition: service_completed_successfully
      embedding-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_started
    environment:
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: semantic_retrieval
      DB_USER: semantic_user
      DB_PASSWORD: semantic_password
      JWT_SECRET: development-secret-key-at-least-32-bytes
      STORAGE_BACKEND: minio
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_BUCKET: document-storage
      MINIO_SECURE: "false"
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: "5672"
      RABBITMQ_USER: guest
      RABBITMQ_PASSWORD: guest
      DOCUMENT_PROCESSING_QUEUE: document_processing
      DOCUMENT_PARSING_QUEUE: document_parsing
      DOCUMENT_EMBEDDING_QUEUE: document_embedding
      QDRANT_HOST: qdrant
      QDRANT_PORT: "6333"
      QDRANT_COLLECTION: document_chunks
      EMBEDDING_MODEL_NAME: sentence-transformers/all-MiniLM-L6-v2
      EMBEDDING_VECTOR_SIZE: "384"
      EMBEDDING_SERVICE_URL: http://embedding-service:8090
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DB: "0"
      QUERY_EMBEDDING_CACHE_TTL_SECONDS: "3600"
      QUERY_EMBEDDING_CACHE_LOGGING: "true"
      CELERY_WORKER_POOL: prefork
      CELERY_WORKER_CONCURRENCY: "2"
      WORKER_ROLE: parsing

  embedding-worker:
    build:
      context: .
    container_name: semantic-retrieval-embedding-worker
    restart: unless-stopped
    command:
      - sh
      - -c
      - >
        celery -A tasks worker
        --loglevel=info
        --pool="$${CELERY_WORKER_POOL}"
        --concurrency="$${CELERY_WORKER_CONCURRENCY}"
        --queues="$${DOCUMENT_EMBEDDING_QUEUE}"
    depends_on:
      bootstrap:
        condition: service_completed_successfully
      embedding-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_started
    environment:
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: semantic_retrieval
      DB_USER: semantic_user
      DB_PASSWORD: semantic_password
      JWT_SECRET: development-secret-key-at-least-32-bytes
      STORAGE_BACKEND: minio
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_BUCKET: document-storage
      MINIO_SECURE: "false"
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: "5672"
      RABBITMQ_USER: guest
      RABBITMQ_PASSWORD: guest
      DOCUMENT_PROCESSING_QUEUE: document_processing
      DOCUMENT_PARSING_QUEUE: document_parsing
      DOCUMENT_EMBEDDING_QUEUE: document_embedding
      QDRANT_HOST: qdrant
      QDRANT_PORT: "6333"
      QDRANT_COLLECTION: document_chunks
      EMBEDDING_MODEL_NAME: sentence-transformers/all-MiniLM-L6-v2
      EMBEDDING_VECTOR_SIZE: "384"
      EMBEDDING_SERVICE_URL: http://embedding-service:8090
      REDIS_ENABLED: "true"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DB: "0"
      QUERY_EMBEDDING_CACHE_TTL_SECONDS: "3600"
      QUERY_EMBEDDING_CACHE_LOGGING: "true"
      CELERY_WORKER_POOL: prefork
      CELERY_WORKER_CONCURRENCY: "2"
      WORKER_ROLE: embedding

  postgres:
    image: postgres:16
    container_name: semantic-retrieval-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: semantic_retrieval
      POSTGRES_USER: semantic_user
      POSTGRES_PASSWORD: semantic_password
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U semantic_user -d semantic_retrieval"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  qdrant:
    image: qdrant/qdrant:latest
    container_name: semantic-retrieval-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

  rabbitmq:
    image: rabbitmq:3-management
    container_name: semantic-retrieval-rabbitmq
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 30s
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

  redis:
    image: redis:7-alpine
    container_name: semantic-retrieval-redis
    restart: unless-stopped
    environment:
      REDIS_MAXMEMORY: 128mb
      REDIS_MAXMEMORY_POLICY: allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    ports:
      - "6379:6379"
    command:
      - sh
      - -c
      - >
        redis-server
        --save ""
        --appendonly no
        --maxmemory "$${REDIS_MAXMEMORY}"
        --maxmemory-policy "$${REDIS_MAXMEMORY_POLICY}"

  minio:
    image: minio/minio:latest
    container_name: semantic-retrieval-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  locust:
    build:
      context: .
    container_name: semantic-retrieval-locust
    profiles:
      - loadtest
    depends_on:
      - nginx
    entrypoint: ["locust", "-f", "load/locustfile.py", "--host", "http://nginx"]
    ports:
      - "8089:8089"

volumes:
  postgres_data:
  qdrant_data:
  rabbitmq_data:
  minio_data:
