events {}

http {
    resolver 127.0.0.11 valid=10s ipv6=off;

    upstream semantic_api {
        zone semantic_api 64k;
        server api:8080 resolve max_fails=2 fail_timeout=10s;
        server api2:8080 resolve max_fails=2 fail_timeout=10s;
        server api3:8080 resolve max_fails=2 fail_timeout=10s;
        keepalive 32;
    }

    server {
        listen 80;
        server_name _;

        client_max_body_size 25m;
        proxy_read_timeout 300s;
        proxy_connect_timeout 30s;
        proxy_send_timeout 300s;

        location / {
            proxy_pass http://semantic_api;
            proxy_http_version 1.1;
            proxy_next_upstream error timeout invalid_header http_502 http_503 http_504;
            proxy_next_upstream_tries 2;
            proxy_next_upstream_timeout 5s;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Connection "";
            add_header X-Upstream $upstream_addr always;
        }
    }
}
